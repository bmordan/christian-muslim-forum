{
  "scripts": {
    "clean": "rm -fr ./dist/* && mkdir ./dist/about ./dist/contact ./dist/events ./dist/people",
    "build": "node ./bin/main.js",
    "copy": "cp -R public/* dist/",
    "serve": "node server.js",
    "start": "npm run clean && npm run copy && npm run build && npm run serve",
    "watch": "onchange './elm/*.elm' './public/*' './bin/*' -v -i -- npm start"
  },
  "dependencies": {
    "dotenv": "^5.0.0",
    "elm-static-html-lib": "^0.1.0",
    "graphql-request": "^1.4.1",
    "ramda": "^0.25.0",
    "string-replace-stream": "0.0.2",
    "body-parser": "^1.18.2",
    "express": "^4.16.2",
    "joi-browser": "^13.0.1",
    "request": "^2.83.0",
    "request-promise": "^4.2.2"
  }
}
