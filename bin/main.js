const build = require('./build')
const tasks = require('./tasks')
const { map } = require('ramda')

map(build, tasks)
// build(tasks[0])
// build(tasks[1])
// build(tasks[2])
// build(tasks[3])
// build(tasks[4])
// build(tasks[5])
